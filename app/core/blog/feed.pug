doctype xml
feed(xmlns="http://www.w3.org/2005/Atom")
  title= blog.title
  - const URL = "https://" + hostname + blog.prefix + "/feed"
  link(href=URL, rel="self")
  link(href=URL)
  updated= posts[0].publish_date
  id= URL
  author
    name Peter Lyons
  each post in posts
    entry
      title= post.title
      link(href="//" + hostname + post.uri)
      updated= post.publish_date
      id= `//${hostname}${post.uri}`
      content(type="html")= post.html || ""
