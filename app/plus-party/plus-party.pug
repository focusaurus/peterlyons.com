extends ../site/layout
block variables
  - title = 'Plus Party: Get Your Addition On!'
block body
  div
    h1= title
    p.
      Paste some numbers into the box.
      It's OK if they have other text around them.
      All the numbers will be parsed out and totaled up for you.
    div.plus-party-container
    img(src='/images/clue_bullets.png', alt='One plus two plus two plus one.')
    iframe(src='//www.youtube.com/embed/O5ROhf5Soqs', width='420', height='315' allowfullscreen)
block append scripts
  script(src="/plus-party.js")
  script.
    Elm.PlusParty.embed(document.querySelector('.plus-party-container'))
    new Clipboard('#copy-fixed') // eslint-disable-line no-new
    new Clipboard('#copy-float') // eslint-disable-line no-new
